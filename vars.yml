packages:
  - mysql-server
  - mysql
  - python3-PyMySQL

services: 
  - mysqld

users:
  - name: "dbuser"
    password: "dbpass"
    priv: '*.*:ALL'
    host: '%'

databases:
  - db1

dump_file: /tmp/databases.sql
